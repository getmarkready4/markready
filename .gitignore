# Raw Cambridge IELTS page scans + OCR (88MB, copyrighted source material).
# The derived corpus lives in markready/src/data and IS committed.
/pdf_pages/

# Env files anywhere in the repo
.env*

# Supabase agent skills: the real files live in .agents/skills (committed);
# .claude/skills holds Windows symlinks that don't survive cross-OS checkout.
# Collaborators regenerate them with `npx skills add` (see skills-lock.json).
/.claude/skills/
